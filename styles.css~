
body { 
  background-color:#000000; 
  color:#CDC9C9;
} 

a:visited,a:link {
  color:#CDC9C9;
}

p {
  display:inline-block;
  text-align:center;

}

.middle-block {
  margin-left:10%;
  margin-right:10%;
  display:block;
  min-width:600px;
  max-width: 1000px;

}

.centered {
  text-align:center;
}

.impressum, .impressum a {
  margin-top:2.5em;
  color:#888484;
}

.caption {
  margin-top:0.15em;
  text-align:center;
}

.mittelgross {
  font-size:1.3em; 
  margin-top:2.6em;
  margin-bottom:0.5em;
  text-align:center;
}

div.team_pic {
 margin-top:10px;
 box-shadow: 0px 0px 15px 5px #CDC9C9;
#  box-shadow: 3px 3px 4px #CDC9C9;
#  border-style: solid;
#  border-width: 0.1em;	
#  border-color: #CDC9C9; 
}

/*ul#nav {
  margin: 0 0 0 200px;
}
*/

ul.drop a { 
  display:block; 
  color: #CDC9C9; 
  font-family: Verdana; 
  font-size: 14px; 
  text-decoration: none;
}

 
ul.drop, ul.drop li, ul.drop ul {
  list-style: none; 
  margin: 0; 
  padding: 0; 
  border: 1px solid #CDC9C9; 
  background: #555; 
  color: #CDC9C9;
}

ul.drop { 
  display: inline-block;
  margin-bottom: 10px;
  margin-top: 12px;
  z-index: 597;
}

ul.drop li {
  float: left;
  line-height: 1.3em;
  vertical-align: middle;
  zoom: 1;
  padding: 5px 10px;
}

ul.drop li.hover, ul.drop li:hover {
  position: relative; z-index: 599;
  cursor: default;
  background: #1e7c9a; 
}

ul.drop ul {
  visibility: hidden;
  position: absolute;
  top: 100%;
  left: -0.1em;
  z-index: 598;
  width: 3.4em;
  background: #555;
  border: 1px solid #CDC9C9; 
}

ul.drop ul li {
  float: none; 
}

ul.drop ul ul {
  top: -2px;
  left: 100%; 
}

ul.drop li:hover > ul {
  visibility: visible 
}